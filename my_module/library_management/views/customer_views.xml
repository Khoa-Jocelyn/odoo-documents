<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<!-- Customer kanban view -->
	<record id="customer_kanban_views" model="ir.ui.view">
		<field name='name'>customer.kanban_views</field>
		<field name="model">library.customer</field>
		<field name='arch' type='xml'>
			<kanban>
				<field name="id" />
				<field name="name" />
				<field name="dob" />
				<field name='age' />
				<field name="gender" />
				<field name='address' />
				<field name="phone" />
				<field name="note" />
				<field name="image" />
				<templates>
					<t t-name="kanban-box">
						<div t-attf-class="oe_kanban_global_click">
							<div class="o_kanban_image">
								<img alt="Avatar"
									t-att-src="kanban_image('library.customer', 
								'image', record.id.raw_value)" />
							</div>
							<div class="oe_kanban_details">
								<ul>
									<li>
										<b>
											Name:
											<field name="name" />
										</b>
									</li>
									<li>
										Birth Day:
										<field name="dob" />
									</li>
									<li>
										Gender:
										<field name="gender" />
									</li>
									<li>
										Age:
										<field name="age" />
									</li>
									<li>
										Phone Number:
										<field name="phone" />
									</li>
								</ul>
							</div>
						</div>
					</t>
				</templates>
			</kanban>
		</field>
	</record>

	<!-- Ticket form view -->
	<!-- <record id="customer_form_views" model="ir.ui.view"> <field name='name'>customer.form_views</field> 
		<field name="model">library.customer</field> <field name='arch' type='xml'> 
		<tree string="Customer"> <field name="name" /> <field name="dob" /> <field 
		name='age' /> <field name="gender" /> <field name='address' /> <field name="phone" 
		/> <field name="note" /> </tree> </field> </record> -->

	<!-- Customer action -->
	<record id="customer_action" model="ir.actions.act_window">
		<field name="name">Customer</field>
		<field name="res_model">library.customer</field>
		<field name="view_mode">kanban,form</field>
		<field name="help" type="html">
			<p class="o_view_nocontent_smiling_face">
				Create your first customer
			</p>
		</field>
	</record>

	<!-- Customer menuitem -->
	<menuitem id="customer_menu" name="Customers"
		parent="ticket_menu_root" action="customer_action" sequence="2" />

</odoo>